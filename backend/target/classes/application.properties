spring.application.name=taskmanager
server.port=8082

# MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://ushathagreat_db_user:ushv@cluster0.lkvgu10.mongodb.net/task_manager_db?retryWrites=true&w=majority
spring.data.mongodb.auto-index-creation=true

# JWT Configuration
app.jwt.secret=MySecretKeyForJWTEncryptionThatIsAtLeast32CharactersLongForHS256Algorithm
app.jwt.expiration=86400000

# CORS Configuration
app.cors.allowed-origins=http://localhost:3000,http://localhost:5173
app.cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
app.cors.allowed-headers=*
app.cors.allow-credentials=true

# Logging
logging.level.root=INFO
logging.level.luv2code.taskmanager=DEBUG